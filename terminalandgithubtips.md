เริ่มจากการใช้งาน Terminal หรือ Powershell 

Terminal คือ หน้าต่างโปรแกรมจำลองที่ให้คุณพิมพ์คำสั่ง (Command) เพื่อสื่อสารกับระบบปฏิบัติการ

Shell คือ โปรแกรมที่อยู่เบื้องหลัง Terminal ทำหน้าที่

* แปลคำสั่งที่พิมพ์
* ส่งต่อให้ระบบปฏิบัติการประมวลผล
* ส่งผลลัพธ์กลับมาแสดง



---

คำสั่ง Terminal / Powershell

คำสั่ง	ตัวอย่าง	ความหมาย
pwd	pwd	เช็คตำแหน่งปัจจุบัน (ฉันอยู่ที่ไหนในระบบ)
ls	ls	ดูรายชื่อไฟล์และโฟลเดอร์ในปัจจุบัน
ls -l	ls -l	ดูรายชื่อไฟล์พร้อมรายละเอียด (ขนาด, วันที่, เจ้าของ)
ls -a	ls -a	ดูไฟล์ทั้งหมด รวมไฟล์ที่ซ่อนไว้ด้วย
ls -al	ls -al	ดูทั้งรายละเอียดและไฟล์ที่ซ่อน
cd	cd desktop	ย้าย path ข้าไปในโฟลเดอร์ชื่อ "desktop"
cd ..	cd ..	ถอยกลับ ออกมา 1 ชั้น 




คำสั่ง	ตัวอย่าง	ความหมาย
echo >	echo "hi" > f.txt	เขียนข้อมูลใหม่ (ลบข้อมูลเก่าทั้งหมด) - Redirection
echo >>	echo "hi" >> f.txt	เขียนต่อท้ายไฟล์ (เก็บข้อมูลเก่า) - Append


คำสั่ง	ตัวอย่าง	ความหมาย
mkdir	mkdir data	สร้างโฟลเดอร์ใหม่ชื่อ "data"
touch	touch note.txt	สร้างไฟล์เปล่าชื่อ "note.txt"


คำสั่ง	ตัวอย่าง	ความหมาย
rm	rm bad_file.txt	ลบไฟล์ (กู้คืนไม่ได้!)
rmdir	rmdir empty_folder	ลบโฟลเดอร์ (ต้องเป็นโฟลเดอร์เปล่าเท่านั้น)
rm -rf	rm -rf folder/	ลบโฟลเดอร์พร้อมทุกอย่างข้างใน (ระวังมาก!)


คำสั่ง	ตัวอย่าง	ความหมาย
cat	cat note.txt	อ่านข้อความในไฟล์แล้วแสดงออกมา


คำสั่ง	ตัวอย่าง	ความหมาย
cp	cp a.txt b.txt	คัดลอก (Copy) ไฟล์ a.txt ไปเป็น b.txt
mv	mv old.txt new.txt	เปลี่ยนชื่อไฟล์จาก old.txt เป็น new.txt
mv	mv f.txt folder/	ย้ายไฟล์ไปยังโฟลเดอร์


---



เริ่มต้นใช้งาน Github

1) ติดตั้ง Git (ครั้งแรกเท่านั้น) ในแพลตฟอร์มที่ใช้ผ่าน terminal เช่น  pycharm , vscode , replit

# ตรวจสอบว่าติดตั้งแล้วหรือยัง
git --version

# ถ้ายังไม่มี ดาวน์โหลดที่: https://git-scm.com/

2) ตั้งค่า Git (ครั้งแรกเท่านั้น)

# ตั้งชื่อ
git config --global user.name "Your Name"

# ตั้งอีเมล (ใช้อีเมลเดียวกับ GitHub)
git config --global user.email "your.email@example.com"

# ตรวจสอบการตั้งค่า
git config --list

---

การใช้งาน Git ในโปรเจ็กต์

ตรวจสอบสถานะ Git

# ดูว่าไฟล์ไหนเปลี่ยนแปลงบ้าง
git status

# ตัวอย่างผลลัพธ์จะเป็น
# Modified:   PageInfo/cookie2.json    (ไฟล์ที่แก้ไข)
# Untracked:  test.py                  (ไฟล์ใหม่ที่ยังไม่ได้ track)

---

Case 1: Push ไฟล์เดียว (เช่น cookie2.json)

# 1 ดูสถานะปัจจุบัน
git status

# 2 เพิ่มเฉพาะไฟล์ที่ต้องการ
git add PageInfo/cookie2.json

# 3 ดูสถานะอีกครั้ง (ตรวจสอบว่าถูกต้อง)
git status
# จะเห็น:
# Changes to be committed:
#   modified:   PageInfo/cookie2.json

# 4 Commit พร้อมข้อความ
git commit -m "Update cookie2.json - refresh session token"

# 5 Push ขึ้น GitHub
git push origin main

# เสร็จสิ้น! ไฟล์ถูก push ขึ้น GitHub แล้ว

---

Case 2: Push หลายไฟล์พร้อมกัน

# 1 ดูสถานะปัจจุบัน
git status

# 2 เพิ่มหลายไฟล์พร้อมกัน
git add PageInfo/cookie.json PageInfo/cookie2.json

# หรือใช้วิธีนี้ (แยกบรรทัด)
git add PageInfo/cookie.json
git add PageInfo/cookie2.json

# 3 ดูสถานะ
git status

# 4 Commit
git commit -m "Update both cookie files - refresh session tokens"

# 5 Push
git push origin main

---

Case 3: Push ทุกไฟล์ที่เปลี่ยน

# 1 เพิ่มทุกไฟล์ที่เปลี่ยน
git add .

# 2 Commit
git commit -m "Update multiple files"

# 3 Push
git push origin main

---

Case 4: Push เฉพาะไฟล์ใน Folder เดียว

# Push ทุกไฟล์ใน PageInfo/
git add PageInfo/
git commit -m "Update PageInfo folder"
git push origin main

# Push เฉพาะไฟล์ .json ใน PageInfo/
git add PageInfo/*.json
git commit -m "Update all JSON files in PageInfo"
git push origin main

---

ไฟล์ที่ไม่ควร Push (Sensitive Files)

# ไม่ควร push ไฟล์เหล่านี้:
- .env                 (Environment variables)
- cookie.json          (Session tokens)
- cookie2.json         (Session tokens)
- db.sqlite3           (Database ตัวพัฒนา)
- __pycache__/         (Python cache)
- *.pyc                (Compiled Python)
- venv/                (Virtual environment)

วิธีป้องกันไฟล์ที่ไม่ต้องการ Push

สร้างไฟล์ .gitignore:

# สร้างไฟล์ .gitignore
touch .gitignore

# เพิ่มเนื้อหาต่อไปนี้ใน .gitignore:

เนื้อหาใน .gitignore:

# Environment
.env
*.env

# Cookies & Sessions
cookie.json
cookie2.json

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
env/

# Database
db.sqlite3
*.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

---

📝 การเขียน Commit Message ที่ดี

✅ แบบที่ดี

git commit -m "Add user authentication feature"
git commit -m "Fix: แก้ไข bug ในหน้า comment dashboard"
git commit -m "Update cookie2.json - refresh session"
git commit -m "Refactor: ปรับโครงสร้าง PageInfo views"
git commit -m "Docs: เพิ่มคู่มือการใช้งาน"

❌ แบบที่ไม่ดี

git commit -m "update"
git commit -m "fix bug"
git commit -m "asdfgh"
git commit -m "test"

คำนำหน้าที่นิยมใช้

feat:     เพิ่มฟีเจอร์ใหม่
fix:      แก้ไข bug
update:   อัพเดทข้อมูล/โค้ด
refactor: ปรับปรุงโค้ดโดยไม่เปลี่ยนฟีเจอร์
docs:     แก้ไขเอกสาร
style:    แก้ไข code style (ช่องว่าง, format)
test:     เพิ่ม/แก้ไขการทดสอบ
chore:    งานเบ็ดเตล็ด (update dependencies)

---

คำสั่ง Git ที่ใช้บ่อย

ดูข้อมูล

# ดูสถานะไฟล์
git status

# ดู commit history
git log

# ดู commit history แบบสั้น
git log --oneline

# ดู commit history พร้อมกราฟ
git log --oneline --graph --all

# ดูความแตกต่างของไฟล์
git diff

# ดูความแตกต่างของไฟล์เฉพาะ
git diff PageInfo/views.py

การย้อนกลับ

# ยกเลิกการแก้ไขไฟล์ (ก่อน add)
git checkout -- PageInfo/views.py

# ยกเลิก git add (ย้ายจาก staging กลับมา)
git reset HEAD PageInfo/views.py

# ยกเลิกการแก้ไขทุกไฟล์
git checkout -- .

# ย้อนกลับไปยัง commit ก่อนหน้า (ระวัง!)
git reset --hard HEAD^

การ Pull & Sync

# ดึงข้อมูลล่าสุดจาก GitHub
git pull origin main

# Fetch + Merge แยกส่วน
git fetch origin
git merge origin/main

---

Branch Management (การจัดการ Branch)

Branch คืออะไร?

Branch = สาขา (แยกการทำงานออกจากหลัก)

ตัวอย่าง:
main          →  →  →  →  (โค้ดหลักที่ใช้งานจริง)
              ↓
feature/login →  →  (ทำฟีเจอร์ login แยก)
              ↓
              ↓ merge
              ↓
main          →  →  →  →  (รวมฟีเจอร์เข้ามา)

คำสั่ง Branch

# ดู branch ทั้งหมด
git branch

# สร้าง branch ใหม่
git branch feature/new-dashboard

# สร้างและสลับไปยัง branch ใหม่
git checkout -b feature/new-dashboard

# สลับ branch
git checkout main

# ลบ branch
git branch -d feature/new-dashboard

# Push branch ขึ้น GitHub
git push origin feature/new-dashboard

# ดึง branch จาก GitHub
git checkout -b feature/new-dashboard origin/feature/new-dashboard

---

การทำงานกับ Remote Repository (GitHub)

เชื่อมต่อกับ GitHub

# ดู remote repository
git remote -v

# เพิ่ม remote (ครั้งแรก)
git remote add origin https://github.com/username/repo-name.git

# เปลี่ยน remote URL
git remote set-url origin https://github.com/username/repo-name.git

Clone Repository

# Clone repo จาก GitHub มาเครื่อง
git clone https://github.com/username/FB_WebApp_Project.git

# Clone และเปลี่ยนชื่อโฟลเดอร์
git clone https://github.com/username/FB_WebApp_Project.git my-project

---

Workflow ทั่วไปในการทำงาน

วันธรรมดา (แก้ไขโค้ด)

# 1. ดึงโค้ดล่าสุดจาก GitHub
git pull origin main

# 2. แก้ไขโค้ด...
# (เขียนโค้ด)

# 3. ดูว่าแก้ไขอะไรบ้าง
git status

# 4. เพิ่มไฟล์ที่ต้องการ commit
git add PageInfo/views.py
git add PageInfo/models.py

# 5. Commit
git commit -m "feat: เพิ่มฟีเจอร์ comment filter"

# 6. Push ขึ้น GitHub
git push origin main

# เสร็จสิ้น!

ทำฟีเจอร์ใหม่ (ใช้ Branch)

# 1. ดึงโค้ดล่าสุด
git pull origin main

# 2. สร้าง branch ใหม่
git checkout -b feature/user-profile

# 3. แก้ไขโค้ด...
# (เขียนโค้ด)

# 4. Commit ใน branch ใหม่
git add .
git commit -m "feat: เพิ่มหน้า user profile"

# 5. Push branch ขึ้น GitHub
git push origin feature/user-profile

# 6. สร้าง Pull Request บน GitHub
# (เข้าไปที่ GitHub → New Pull Request)

# 7. หลังจาก merge แล้ว กลับมา main
git checkout main
git pull origin main

# 8. ลบ branch เก่า (ถ้าต้องการ)
git branch -d feature/user-profile

---

ตัวอย่างการใช้งานจริงในโปรเจ็กต์

Case 1: อัปเดต Cookie Files

# สถานการณ์: แก้ไข cookie2.json เพื่อรีเฟรช session

# 1. ดูสถานะ
git status
# Modified: PageInfo/cookie2.json

# 2. เพิ่มไฟล์
git add PageInfo/cookie2.json

# 3. Commit
git commit -m "update: refresh cookie2.json session token"

# 4. Push
git push origin main

# Done!

Case 2: แก้ไข Models และ Views

# สถานการณ์: เพิ่ม field ใหม่ใน models.py และแก้ไข views.py

# 1. ดูสถานะ
git status
# Modified: PageInfo/models.py
# Modified: PageInfo/views.py

# 2. เพิ่มทั้งสองไฟล์
git add PageInfo/models.py PageInfo/views.py

# 3. Commit
git commit -m "feat: add post_saved field to FBCommentDashboard model"

# 4. Push
git push origin main

Case 3: แก้ไข Template

# สถานการณ์: แก้ไข comment_dashboard.html

# 1. เพิ่มไฟล์
git add templates/PageInfo/comment_dashboard.html

# 2. Commit
git commit -m "fix: แก้ไข UI comment dashboard - ปรับปุ่ม filter"

# 3. Push
git push origin main

---

แก้ปัญหาที่เจอบ่อย

ปัญหา 1: Merge Conflict

# เกิดเมื่อมีคนแก้ไขไฟล์เดียวกัน

# 1. ดึงโค้ดล่าสุด
git pull origin main

# 2. เจอ conflict
# CONFLICT (content): Merge conflict in PageInfo/views.py

# 3. แก้ไขไฟล์ที่ conflict (เปิดไฟล์ดู)
# <<<<<<< HEAD
# โค้ดของคุณ
# =======
# โค้ดจาก GitHub
# >>>>>>> origin/main

# 4. ลบ marker (<<<, ===, >>>) แล้วเก็บโค้ดที่ต้องการ

# 5. เพิ่มไฟล์ที่แก้แล้ว
git add PageInfo/views.py

# 6. Commit
git commit -m "fix: resolve merge conflict in views.py"

# 7. Push
git push origin main

ปัญหา 2: ลืม Pull ก่อน Push

# Error: Updates were rejected because the remote contains work

# แก้ไข:
# 1. Pull ก่อน
git pull origin main

# 2. แก้ conflict (ถ้ามี)

# 3. Push อีกครั้ง
git push origin main

ปัญหา 3: Commit ผิด ต้องการแก้ไข

# แก้ไข commit message ล่าสุด (ยังไม่ push)
git commit --amend -m "ข้อความใหม่"

# แก้ไขไฟล์ใน commit ล่าสุด (ยังไม่ push)
git add PageInfo/views.py
git commit --amend --no-edit

#หลัง push แล้ว ไม่แนะนำให้แก้ไข

ปัญหา 4: Push ไฟล์ที่ไม่ควร Push

# ถ้ายังไม่ push
git reset HEAD cookie.json

# ถ้า push แล้ว
git rm --cached cookie.json
git commit -m "remove: ลบ cookie.json ออกจาก repo"
git push origin main

# เพิ่มใน .gitignore เพื่อป้องกันในอนาคต
echo "cookie.json" >> .gitignore

---

การใช้ GitHub Desktop (GUI Alternative)

ติดตั้ง GitHub Desktop

ดาวน์โหลด: https://desktop.github.com/

ข้อดี GitHub Desktop

* เห็นภาพ (GUI) ไม่ต้องพิมพ์คำสั่ง
* คลิกเลือกไฟล์ได้
* ดู diff (ความแตกต่าง) ได้ง่าย
* จัดการ branch ง่าย

ขั้นตอนการใช้ GitHub Desktop

1. เปิด GitHub Desktop
2. File → Add Local Repository → เลือก FB_WebApp_Project
3. เห็นไฟล์ที่เปลี่ยนแปลงทางซ้าย
4. เลือกไฟล์ที่ต้องการ commit (tick)
5. เขียน commit message ล่าง
6. กด "Commit to main"
7. กด "Push origin" ขวาบน
8. ✅ เสร็จสิ้น!

---

🎯Best Practices

✅ ควรทำ

1. Commit บ่อยๆ - ทุก 1-2 ชั่วโมง หรือทุกครั้งที่ทำฟีเจอร์เล็กเสร็จ
2. Pull ก่อน Push - เพื่อหลีกเลี่ยง conflict
3. เขียน commit message ให้ชัดเจน - อธิบายว่าทำอะไร
4. ใช้ .gitignore - ป้องกันไฟล์ที่ไม่ต้องการ
5. ใช้ branch - สำหรับฟีเจอร์ใหม่
6. Review code ก่อน push - ใช้ git diff ดูก่อน
7. Test ก่อน push - รันโปรแกรมให้แน่ใจว่าไม่มี error

❌ ไม่ควรทำ

1. ไม่ commit ไฟล์ลับ - .env, cookie.json, API keys
2. ไม่ push --force - ระวังทำข้อมูลหาย
3. ไม่ commit ทั้งหมดรวดเดียว - แยก commit ตามฟีเจอร์
4. ไม่เขียน commit message ไม่ชัด - เช่น “update”, “test”
5. ไม่ทำงาน direct บน main - ใช้ branch สำหรับฟีเจอร์ใหม่

---

Git Commands Cheat Sheet

┌─────────────────────────────────────────────────────────────────┐
│                      คำสั่ง Git ที่ใช้บ่อย                     │
├─────────────────────────────────────────────────────────────────┤
│ # Setup                                                          │
│ git config --global user.name "Your Name"                        │
│ git config --global user.email "email@example.com"               │
│                                                                  │
│ # Status & Info                                                  │
│ git status              → ดูสถานะไฟล์                            │
│ git log --oneline       → ดู commit history                     │
│ git diff                → ดูความแตกต่าง                          │
│                                                                  │
│ # Add & Commit                                                   │
│ git add file.py         → เพิ่มไฟล์เดียว                         │
│ git add .               → เพิ่มทุกไฟล์                            │
│ git commit -m "msg"     → Commit พร้อมข้อความ                   │
│                                                                  │
│ # Push & Pull                                                    │
│ git pull origin main    → ดึงโค้ดล่าสุด                          │
│ git push origin main    → Push โค้ดขึ้น GitHub                  │
│                                                                  │
│ # Branch                                                         │
│ git branch              → ดู branch ทั้งหมด                      │
│ git checkout -b new     → สร้าง branch ใหม่                      │
│ git checkout main       → สลับไป main                            │
│ git merge feature       → รวม branch                             │
│                                                                  │
│ # Undo                                                           │
│ git checkout -- file    → ยกเลิกการแก้ไข                         │
│ git reset HEAD file     → ยกเลิก git add                         │
│ git reset --hard HEAD^  → ย้อนกลับ commit ล่าสุด (ระวัง!)       │
└─────────────────────────────────────────────────────────────────┘

---

📚 Resources

* Git Documentation
* GitHub Guides
* Git Visual Cheat Sheet
* Learn Git Branching

---

สรุป

เริ่มต้นใช้งาน

# 1. ตั้งค่า Git
git config --global user.name "Your Name"
git config --global user.email "your@email.com"

# 2. Clone โปรเจ็กต์
git clone https://github.com/username/FB_WebApp_Project.git

# 3. ทำงานกับโค้ด
cd FB_WebApp_Project
# แก้ไขโค้ด...

# 4. Push การเปลี่ยนแปลง
git add .
git commit -m "feat: add new feature"
git push origin main

ใช้งานประจำวัน

# เช้า: ดึงโค้ดล่าสุด
git pull origin main

# ทำงาน: แก้ไขโค้ด
# ...

# เย็น: Push โค้ด
git add .
git commit -m "update: today's work"
git push origin main

